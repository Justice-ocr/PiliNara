# Windows sync — 2026-09-10

- Windows starting point: `2d7571029`
- Integration source: fork `main` at `c5369b62d`
- Included upstream main: `3cc0cbc26`

## Inherited

- Reply cursor API, LaTeX-to-Unicode input, account removal fixes, AI Markdown
  fixes, picture adjustments, timer countdowns and updated media dependencies.
- Missing video-quality supplementation and preferred codec selection.
- Live keyword/user blocking and bounded chat retention.
- Favorite page ordering and follow-group ordering.

## Adapted for Windows

- Preserve Neo shell, workspace tabs, split views, controller identities,
  context-panel close handlers, font import and existing Windows navigation.
- Host the new independent dynamic like/repost lists inside the existing
  reaction sidebar (or its compact popup); do not replace the Windows page.
- Retain Duration-based desktop progress, hover preview and PiP initialization
  while adding upstream background-audio recovery callbacks.
- Keep live chat source indices stable while hiding SC in the separate chat
  pane, so upstream trimming does not invalidate the Windows SC split layout.
- Keep note creation under its enclosing Windows Scaffold and retain the
  login check.
- Use the shared follow sort controller with a Windows header action; keep
  scoped controller tags.
- Keep Flutter 3.47.2 and Starfallan/getx dev. Resolve the material_ui patch
  target from package_config.json, without deleting cached dependencies or
  rewriting an unrelated GetX checkout.
- Register the workspace's missing global route aliases and map blockSetting
  to the actual blocking settings page.

## Deliberately not exposed

- Android/iOS/Linux project and packaging changes are not restored on this
  Windows-only branch. Linux WebView support remains shared source only.
- The removed legacy live level/rank/phone-verification controls are not
  resurrected: upstream replaced that implementation with keyword/user rules.
- Upstream mobile-oriented page layout and integer seek-state APIs do not
  replace the Windows presentation/desktop progress implementation.

## Validation

No manual clicking or interactive UI verification. The integration is checked
with Dart analysis (zero compile errors; existing lint findings remain) and
the four focused Windows workspace test files (21 tests passed). The dependency
patch preparation script was also exercised successfully. Native Windows
packaging and release publication are separate actions.
